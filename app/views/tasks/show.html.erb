<%= link_to 'Back',list_path(params[:list_id]), class: 'btn btn-warning' %>
<%= link_to 'Edit', edit_task_path(@task), class: 'btn btn-info' %>
<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
<p id="notice"><%= notice %></p>


<table class="table table-striped table-bordered">
<tr>
     <th>List:</th>
     <td><%= @task.list.title %></td>
</tr>

<tr>
	<th>Task:</th> 
 	<td><%= @task.task %></td>
</tr>

<tr>
            <th>Description:</th>
	<td><%= @task.description %></td>
</tr>

<tr>
 <th>Due Date:</th>
 <td><%= time_tag @task.date %></td>
</tr>

</table>
		</div>
	</div>
</div>